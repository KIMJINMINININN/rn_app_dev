/**
 * widgets/app-shell 공개 API (Develop §6.3).
 * 앱 레이어가 AppShell만 가져다 쓰도록 단일 진입점으로 노출.
 */
export { AppShell } from './AppShell';
export { NAV_ITEMS, isNavItemActive } from './nav-items';
export type { NavItem } from './nav-items';
