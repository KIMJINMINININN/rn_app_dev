/**
 * widgets/technique-editor 공개 API (F4-AC1).
 * TechniqueForm — 기술 생성/편집 폼(feature edit-technique + media-upload + tag-filter 조합).
 * app((app)/techniques/{new,[id]/edit})는 이 배럴만 import 한다.
 */
export { TechniqueForm, type TechniqueFormProps } from './ui/TechniqueForm';
